from face_normalizer import *


def find_faces(image):
    """
    Returns the faces on the image
    :param image: the image
    :return: the faces list formatted as [(x, y, w, h), (x, y), (x, y)]
    """

    faces = []  # face format : ((x, y, width, height), position_eye_left, position_eye_right)
    # To complete ----------------------------------

    return faces

